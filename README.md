Copy SOURCE to DEST

Usage:
```
  rcp [flags]
```

Examples:
```
rcp SOURCE DEST
```

Flags:
```
  -h, --help           help for rcp
      --parallel int   The number of file transfers to run in parallel. (default 6)
  -v, --verbose        verbose output
```
